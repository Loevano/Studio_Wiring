# Defaults

Reusable defaults and templates.

## Folders
- `device_templates/`: reusable device objects you can copy into studio models.
- `default_template/`: clean empty model/patch starting point.
