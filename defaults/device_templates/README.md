# Device Templates

- `studio_sidecar_common_devices.json`: device templates exported from the current Studio Sidecar model.
- `minimal_device_template.json`: one clean starter device object.

Use templates by copying objects into `devices[]` in your target model JSON.
